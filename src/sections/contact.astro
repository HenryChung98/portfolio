<!-- ---
import { PROFILE } from "../consts";
import SectionTitle from "../components/section-title.astro";



const availability = {
  status: "Open to opportunities",
  responseTime: "Usually responds within 24 hours"
};
---

<SectionTitle title="Contact" />

<div class="mb-4 p-4 bg-primary/10 rounded-lg border border-primary/20">
  <div class="flex items-center gap-2 mb-1">
    <div class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
    <span class="text-sm font-semibold">{availability.status}</span>
  </div>
  <p class="text-xs text-muted-foreground">{availability.responseTime}</p>
</div>

<div class="mb-6 space-y-3">
  {contactInfo.map((item) => (
    <div class="flex items-center gap-3 group">
      <div class="bg-primary/20 p-2 rounded-lg group-hover:bg-primary/30 transition-colors duration-200">
        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
          <path d={item.icon} />
        </svg>
      </div>
      <div class="text-sm">
        <div class="font-semibold text-foreground/80 text-xs mb-0.5">{item.label}</div>
        {item.href ? (
          <a href={item.href} class="text-muted-foreground hover:text-foreground transition-colors duration-200">
            {item.value}
          </a>
        ) : (
          <span class="text-muted-foreground">{item.value}</span>
        )}
      </div>
    </div>
  ))}
</div> -->